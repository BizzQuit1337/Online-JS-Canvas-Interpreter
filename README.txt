Author: Albert Brown

This is an online interpreter for JavaScript Canvas. The idea was for the company ETC Group so they could teaching
games development on canvas across differnt schools without having to setup lots of different computers.

The brickbreaker file is example code to run a brickbreaker game. Currently the page has no style sheet as that was added with the Python interpreter 
directly on the wordpress website.